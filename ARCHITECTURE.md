# xxtrain 架构

> 本文只记录长期方向、核心边界和当前阶段的架构。具体接口、配置字段与算法参数随实现演进，在对应模块中定义。

## 1. 项目定位

xxtrain 面向垂类视觉任务。现有项目已经能够把外部标注转换为可训练数据集，并完成模型训练、检查和导出；下一阶段将其发展为供内部现场实施人员使用的自助训练平台。

平台的目标是让用户通过一个统一入口完成固定任务的图片上传、标注、训练进度查看和模型获取。管理员不再逐次整理图片、分发标注或执行训练，只负责定义任务模板、定期维护基准数据和处理少量异常情况。平台不承诺减少完成一次训练所需的标注量，而是把原本集中在管理员身上的数据准备、标注推进和训练执行责任分散给各现场实施人员。

xxtrain 不建设通用 AutoML、通用数据平台或完整 MLOps。它只服务开发者预定义的垂类任务模板，每个训练任务只训练一个模型；标注与训练执行分别复用 CVAT 和 ClearML，xxtrain 保持整个业务流程、数据和任务状态的控制权。

### 1.1 当前进度快照

| 能力域 | 状态 | 当前边界 |
| --- | --- | --- |
| 基础数据模型与格式 I/O | 完成 | LabelImg、LabelMe、YOLO、COCO 已接入统一 `Annotation`，支持各格式可表达范围内的读写与 round-trip |
| typed 数据转换管线 | 完成 | 标准 detect、segment、pose、obb、classify 与现有复合任务均通过 `Source -> Pipeline -> Sink` 执行 |
| 训练、导出与结果检查 | 完成 | Scenario 驱动的训练、独立 ONNX 导出、预测可视化、分类有标签及无标签目录 review 已可用 |
| 打包与安装式 CLI | 完成 | setuptools、`xxtrain train/export/review` 和用户级权重缓存已建立 |
| 按图片发现标注文件 | 完成 | 固定布局下按图片同名发现 YOLO、LabelImg、LabelMe 候选文件，并在任务边界校验 |
| 目录标注目标子集转换与统计 | 完成 | `labels.txt` 保持目标类别所有权；转换报告来源和过滤标签，并校验最终类别覆盖 |
| 三方可行性验证 | 完成 | 已以单类别 `point-detect` 打通 CVAT 标注、COCO 导出、xxtrain 转换、ClearML 队列执行、真实 GPU 训练和 ONNX 产物取回 |
| 平台数据与用户闭环 | 已规划 | 尚无统一入口、SQLite 元数据、单份图片存储、工作区、基准数据和自动清洗 |

### 1.2 平台总体边界

平台围绕开发者预定义的任务模板组织。普通用户只能选择模板和固定训练预设，不能创建任务结构、编辑底层超参数或组合多模型工作流。Point 的检测、分类和分割仍是三个独立模板；多级业务通过前置标注要求表达，不建设自动模型 DAG。

组件长期职责如下：

- **xxtrain 平台**：唯一用户入口、任务模板、工作区、数据集、训练快照和跨平台状态；
- **xxtrain 训练核心**：垂类数据转换、临时裁剪、训练配置、训练和模型导出；
- **CVAT**：由 xxtrain 调用的标注与审核引擎；
- **ClearML**：由 xxtrain 调用的训练执行、记录、队列和未来多机调度引擎；
- **SQLite 与文件存储**：平台元数据和单份权威原图；
- **FiftyOne**：第一版不引入。

用户正常路径为：

```text
选择任务模板
  → 进入当前现场工作区
  → 上传并自动清洗图片
  → 标注或审核
  → 提交单模型训练
  → 查看进度
  → 获取模型
```

管理员只定期按任务模板检查各用户积累的数据并提升合格样本，不作为用户继续标注或训练的前置条件。

### 1.3 可行性依据

本规划能够保持可控，依赖以下已经明确的范围约束：

- 现有 xxtrain 已经完成垂类数据转换、训练、检查和导出，平台不需要重新验证训练核心本身；
- 标注画布和训练执行分别复用 CVAT 与 ClearML，xxtrain 只建设固定业务闭环和必要的数据控制面；
- 初期只供内部人员单机使用，任务由开发者预定义，普通用户不能扩展工作流或训练参数；
- 典型任务只有一两千张垂类图片，管理员保留低频兜底，不追求无人干预的通用自动化；
- 本规划假设同一任务模板下的数据允许跨用户用于训练和基准积累；如果实际数据权属不允许共享，必须重新引入用户或客户级数据边界；
- 自动清洗、自助标注和训练编排能够把日常推进责任交给现场实施人员，平台价值来自消除管理员这一单点瓶颈，而不是减少总标注工作量；
- P1 已在未建设平台数据和 UI 的前提下验证三方真实链路，证明组件组合成立；后续产品化从 P2 的数据基础开始。

## 2. 基础训练核心（已完成）

### 2.1 核心流程

```text
任务定义
  → 读取图像与外部标注
  → 转换为统一标注对象
  → 按任务组合处理管线
  → 生成训练集与模型配置
  → 训练 / 验证 / 导出
```

基础训练核心不包含平台标注闭环，也不负责标注 GUI。人工标注继续由 labelimg、labelme、X-AnyLabeling 等外部工具完成。

### 2.2 核心组成

#### Scenario 组合根

`xxtrain.task.TaskType` 只描述 detect、segment、pose、classify、obb 五种基础模型类型。Python Scenario 是具体数据集转换与训练的组合根：

```text
Scenario file
├── DatasetRecipe: labels + Pipeline + Sink
├── model version/scale
├── split + reserve_no_label=False
└── YOLO.train() overrides
```

Scenario 文件必须导出 `SCENARIO: TrainingScenario`。其中 `DatasetRecipe` 明确数据集名、基础 `TaskType`、标签目录、Pipeline 和 Sink；`train_args` 只覆盖源码提供的标准训练参数，并原样传给 `YOLO.train()`。

所有相对 `Path` 值以 Scenario 文件所在目录解析，普通相对字符串不做路径转换。该目录同时定义固定布局：

```text
<scenario_dir>/
├── <scenario>.py
├── src/                    # 原始图像与标注
├── <recipe.name>/          # 可重新生成的数据集产物与模型 YAML
└── weights/                # ONNX 与分类参考图
```

`standard_recipe()` 由 `xxtrain.pipeline.recipes` 提供，负责 detect、segment、pose、obb、classify 五个标准 Recipe。point、knob、light 七个特殊 Recipe 直接由对应 `data/` Scenario 文件组合；具体 Processor 和 Sink 不是承诺给任意第三方插件的稳定 API。

配置不需要描述所有处理细节。通用差异进入配置；特殊几何处理或业务转换继续由代码实现，避免把配置发展成另一套编程语言。

#### 统一标注表示

外部标注格式先解析为内部统一的 `Annotation`。后续处理只依赖统一对象，不直接耦合 labelimg XML、labelme JSON 或具体训练格式。

统一表示负责表达标签、形状、坐标和实例关系。LabelImg、LabelMe、YOLO 和 COCO 在格式可表达且任务语义允许的范围内通过同一表示读写；无法无损表达的转换在边界明确失败。置信度、来源等字段在模型候选标签真正接入平台时再扩展，不在基础训练核心中预设完整 schema。

长期数据契约如下：

- Annotation、几何值和 pipeline 记录保持不可变，变换返回新对象；
- 标签目录保持显式声明或已定义发现规则产生的稳定顺序，数字形式的名称仍按字符串处理；
- 坐标、几何运算和虚拟裁剪范围使用浮点数，不在 data、Source、Processor 或编码阶段提前截断；
- 整数化只允许发生在 OpenCV 数组切片等明确栅格边界；
- 格式无法无损表达、几何不符合任务或标签不属于当前目录时尽早失败。

#### 可组合转换管线

标注转换采用 `Source -> Pipeline -> Sink`。`DirectorySource` 按稳定顺序发现样本；不可变的 `Sample`、`ImageRef` 和阶段专用 `*Input` / `*Output` 记录在 Processor 之间传递，不使用共享 payload 字典；Sink 是唯一的数据集写入边界。主要职责包括：

- 解析图像与标注；
- 过滤和变换标注；
- 匹配父子实例；
- 裁剪复合任务的局部区域；
- 生成训练标签并划分数据集。

对目录源，标注发现以图片为中心并遵循固定同名布局：`imgs/<name>.*` 分别映射到 `labels/<name>.txt`、`anns/<name>.xml` 和 `anns_seg/<name>.json`。缺失候选文件表示该格式没有标注；不会递归搜索任意目录，也不会为同一格式加载多个候选文件。Pose 可按已定义语义组合 LabelImg 框与 LabelMe 关键点；其他任务遇到多个非空标注格式时拒绝歧义输入。

“发现标注文件”不等于“发现类别目录”。普通目录源不扫描所有 XML/JSON 汇总 `LabelCatalog`；标准任务始终以 `src/labels.txt` 为类别名称与顺序的真值来源。转换在同一次遍历中统计全部来源标签、过滤并报告非目标标签，再按完整处理链之后的最终类别验证目标覆盖；它不会自动扩展类别目录。COCO 可从自身 category schema 提供目录，特殊 Recipe 则显式携带固定目录和转换规则。

`ItemProcessor` 表示一对零或一，`ExpandProcessor` 表示一对多。每张图像和每个裁剪实例都使用独立值对象，裁剪内容延迟到 Sink 才物化，避免跨样本状态泄漏。Processor 保持单一职责；recipe 负责组合，不把任务分支堆入训练入口。

#### 数据集产物

转换结果是训练框架可直接消费的数据集，包括图像、标签、训练/验证划分和数据集描述文件。它们是可重新生成的构建产物，不是原始标注的替代品。

相同原始数据、任务定义和代码版本应产生一致结果。转换中遇到缺失输入、非法形状或尺寸不一致时应尽早失败，避免错误进入训练阶段。

#### 训练入口

训练层负责串联 Scenario 加载、数据转换、模型配置、预训练权重、训练和 ONNX 导出。当前以 Ultralytics YOLO 为唯一训练后端，保持薄封装。

安装后通过统一的 `xxtrain` 命令执行三个独立职责：

- `xxtrain train`：运行完整训练工作流；
- `xxtrain export`：从已有 checkpoint 独立导出 ONNX；
- `xxtrain review`：运行预测并保存可视化结果或分类错分报告。

预测结果检查不是 `model.val()` 指标评估。训练过程中的 Ultralytics 验证仍负责指标、曲线和样例；当前没有单独重新计算验证指标的入口。

只有出现第二个真实训练后端并确认公共边界后，才抽象统一后端接口。现阶段不为假设中的框架兼容性增加复杂度。

### 2.3 模块边界

当前代码的职责划分如下：

- `xxtrain.task`：只定义五种基础 `TaskType`；
- `xxtrain.data`：不可变标注、LabelImg/LabelMe/YOLO/COCO 格式 I/O、几何和数据集产物工具；
- `xxtrain.pipeline.discovery`：稳定顺序的样本发现；
- `xxtrain.pipeline.core` / `processors`：typed records、Pipeline 和具体转换步骤；
- `xxtrain.pipeline.recipes`：`DatasetRecipe` 和五条标准 Recipe；
- `xxtrain.pipeline.sinks` / `workflow`：数据集写入边界和 `convert_dataset()` 公共入口；
- `xxtrain.training.scenario`：`TrainingScenario`、Python Scenario 加载和相对 `Path` 解析；
- `xxtrain.training.model` / `workflow`：模型 YAML、预训练权重和固定训练工作流；
- `xxtrain.training.exporting` / `review`：ONNX 导出、分类参考图和预测结果检查；
- `xxtrain.cli`：安装后的 `train`、`export`、`review` 子命令入口。

项目通过 setuptools 按 `src` 布局安装，要求 Python 3.11 及以上。预训练权重位于 `platformdirs` 提供的 xxtrain 用户缓存目录；具体数据集的 Scenario 仍留在数据集目录，不作为包资源安装。

旧的 `annparser.py`、`annprocessor.py`、`annconverter.py` 已在 typed pipeline 承接全部既有任务后删除。

`xxtrain.pipeline` 包级受支持 API 为 `Context`、`ConversionConfig`、`ConversionReport`、`DatasetRecipe`、`ExpandProcessor`、`ImageRef`、`ItemProcessor`、`Pipeline`、`Sample`、`convert_dataset` 和 `standard_recipe`。`convert_dataset(recipe, root_path, *, split=10, reserve_no_label=False)` 直接接收 Recipe，不保留旧任务名注册表或旧转换签名。

`xxtrain.training` 包级受支持 API 为 `TrainingScenario`、`load_scenario`、`train`、`export` 和 `review`。模型模板处理、预训练下载、分类错分整理等细节保持内部实现。

### 2.4 基础核心完成标准

- 标准任务和现有复合任务都通过统一管线构建数据集；
- 新增常规任务主要通过组合已有 Processor 完成；
- 转换结果可复现，关键解析与几何转换可独立验证；
- 训练入口只负责流程编排，不承载标注处理逻辑；
- 训练、验证和 ONNX 导出形成稳定可执行路径。

## 3. 平台演进路线

### 3.1 P1：xxtrain、CVAT、ClearML 最小可行性验证（已完成）

平台第一阶段没有建设产品功能，只回答一个决定后续路线是否成立的问题：xxtrain 能否以最小编排打通 CVAT 的标注结果和 ClearML 的训练执行。

使用一个固定任务模板和一份受控小数据集验证：

```text
xxtrain 创建 CVAT 标注任务并提供图片
  → 人工完成标注
  → xxtrain 取回标注结果
  → xxtrain 转换为训练数据
  → xxtrain 提交 ClearML 任务
  → ClearML Agent 执行真实 xxtrain 训练命令
  → xxtrain 查询运行状态并取得模型产物
```

验证使用 4 张图片和单类别 `point-detect` 任务：CVAT Job 完成人工框标注后导出 COCO，xxtrain 将其转换为 3 张训练图片和 1 张验证图片的数据集，ClearML Agent 从队列领取任务并执行真实 xxtrain GPU 训练，最终 ONNX 由 ClearML 保存并可通过接口取回，取回文件通过 ONNX 结构校验。三方职责、数据语义和调用链均成立，平台可以进入 P2。

P1 同时确认，三方连通不等于用户流程已经成立。CVAT 的原生完成入口较隐蔽，完成后不会自动返回业务流程；ClearML 页面包含远超普通用户所需的实验管理信息。因此 xxtrain 必须掌握步骤门禁、状态轮询和页面跳转，不能让普通用户自行在多个第三方页面间推进任务。P1 未实现统一 UI、多用户、SQLite 数据管理、自动清洗、基准数据、工作区或多机调度，这些仍按后续阶段逐项设计。

### 3.2 P2：平台数据基础

可行性通过后，建立由 xxtrain 掌握的数据基础：

- 用 SQLite 保存任务模板、用户、工作区、标注状态和外部任务映射；
- 用内容寻址文件存储保证权威原图只保存一份；
- 每个用户、每个任务模板、每个现场使用一个极简工作区，用户之间相互隔离；
- 每个任务模板拥有对用户隐藏、跨用户共享的基准数据；
- 为用户移除和后续自动过滤保留独立逻辑状态，不立即删除物理图片；
- 提交训练时冻结图片、标签、模板和代码边界，后续编辑只影响下一次训练。

本阶段建立权威数据和生命周期，不扩展成通用数据版本、项目管理或权限系统。

### 3.3 P3：用户自助闭环

在稳定数据边界上实现普通用户的统一路径：

```text
选择模板
  → 进入现场工作区
  → 上传并自动清洗
  → 标注或审核
  → 提交训练
  → 查看进度
  → 获取模型
```

xxtrain 是唯一业务入口。CVAT 只承担当前标注步骤，ClearML 在后台承担排队、执行、记录和产物保存。正常路径不要求管理员整理图片、分发标注、启动训练或反馈进度。

### 3.4 P4：基准数据积累

管理员按任务模板低频检查各用户积累的数据，将合格图片和标签提升到隐藏基准数据。提升只增加数据关系，不复制图片，也不阻塞用户继续训练。

所有使用同一模板的训练都会静默加入该模板当时的基准数据和当前现场补充数据。基准积累的目标是提高后续训练稳定性，其实际效果需要由后续真实任务验证。

### 3.5 P5：按需扩展闲置算力

只有出现真实排队或算力利用需求时，才为闲置机器增加 ClearML Agent。多台机器分别执行独立的训练、预标注或数据处理任务，共享中央数据存储；不为单次训练建设跨机器分布式训练。

本阶段不改变用户流程，也不让训练节点直接拥有或修改平台权威数据。P5 是由真实算力需求触发的可选扩展。

## 4. 已确认的重大决策

### 4.1 产品与组件边界

- xxtrain 是普通用户唯一入口，并拥有任务、数据和运行状态的业务真值；CVAT 和 ClearML 是由它调用的专业执行引擎。
- 不维护 CVAT 或 ClearML 的中文分支。ClearML 不向普通用户开放；CVAT 只暴露完成当前标注所需的最小界面，其进入、提交和返回路径由 xxtrain 承接。
- 平台初期只供内部人员使用，只保留普通用户和管理员所需的最小身份边界，不建设商业多租户、复杂鉴权、计费或通用工作流系统。
- 普通用户只能选择开发者预定义的任务模板和训练预设，不能创建任务结构、编辑标签 schema 或直接修改底层训练参数。
- 第一版不引入 FiftyOne；自动清洗先覆盖普通上传路径，管理员高级数据探索等真实需要出现后再重新评估。

### 4.2 多级任务的拆分与依赖

- 每个训练任务只训练一个模型。一个业务需要多个模型时，用户必须分别提交多个训练任务，平台不建设自动多模型 DAG。
- 多级业务中的各个模型仍是独立任务。下级模型任务依赖完整、合格的上级标签，不依赖平台内存在上级训练任务、上级 checkpoint 或上级模型执行历史。
- 下级任务的标注明确分为两个步骤：先取得并逐图确认全部上级标签，再标注当前模型需要学习的目标标签；两个步骤使用同一原图和权威标注，不为下级任务管理一套裁剪图片数据集。
- 下级任务要求所有上级标签逐图完成并经人工确认；上级标签不是当前模型的学习目标，不能依赖当前训练过程生成。
- 进入训练快照的样本必须完整标注当前模型需要学习的目标。尚未完成目标标注的图片或实例可以继续留在工作区，但不得进入训练，也不得被解释为负样本。
- 上级标签可以通过人工标注、现有数据集导入或已有模型预测取得。模型预测只产生候选标签，必须完成人工审核后才能满足下级任务门禁。
- 当上级标签不完整或质量不合格时，平台阻止下级训练并提示用户先处理上级任务；平台不自动补救错误使用方式，也不隐式启动其他模型训练。

### 4.3 Point 任务语义

- `point-detect`、`point-classify` 和 `point-segment` 是三个独立单模型模板。分类与分割处于同一级，二者都只依赖完整、逐图确认的 Point 框，彼此不依赖。
- 一级标签以整张原图为完成单位：必须检查已有框、补充漏框，并明确确认没有 Point 的图片；只审核模型已经发现的框不算完成。
- 同一个 Point 框只保存一次。`Point` 表示一级框已经确认但二级分类尚未标注，`tl/tc/cl/cc` 表示已经具有二级分类的 Point 框。
- 检测任务把 `Point/tl/tc/cl/cc` 统一投影为 `Point`；分类任务只使用 `tl/tc/cl/cc` 作为目标类别。保留为 `Point` 的框表示分类标注尚未完成，在完成前不进入分类训练，也不是负样本。
- 分割任务同样使用完整、可信的 Point 框确定二级目标范围，二级分割标签单独表达。合法框暂时没有分类或分割标签只表示尚未标注，不表示不存在二级目标。
- Point 一级框禁止重叠。每个合法一级框都存在真实二级目标；如果确认不存在二级目标，说明一级框本身错误。
- 原图和原图坐标标签是权威数据。分类或分割需要的裁剪图只在预览和训练时临时生成，不纳入数据集管理。

### 4.4 数据集、工作区与存储

- 每个任务模板拥有一套对普通用户隐藏、跨用户共享的基准数据。训练静默组合提交时的基准数据与当前用户、当前现场的有效补充数据。
- 基准数据的适用范围由任务模板负责。如果不同现场的数据分布不应共同训练，应拆成不同任务模板，而不是增加用户可配置的数据选择或按用户维护基准数据。
- 用户上传数据按用户和现场隔离。一个现场对应一个极简工作区，同一工作区可多次上传、标注和训练；开始新现场时归档旧工作区。
- 管理员按任务模板低频检查用户积累的数据，将合格图片和标签提升到基准数据；提升不阻塞用户训练，也不复制图片。
- SQLite 保存元数据和逻辑关系，权威原图使用内容寻址存储并只保存一份。工作区、基准数据和训练快照都引用物理图片，不分别维护副本。
- 不保留 `upload_batch_id`。多次上传直接追加到当前工作区，只有标注或训练需要固定输入时才生成快照。
- 用户移除、模糊过滤、相似度过滤和标注状态是相互独立的语义。移除或过滤不立即物理删除图片，也不能覆盖已有正式标签。
- 正式数据只包括原图、人工标签、人工审核通过的模型候选标签、任务模板及其关系。未审核模型候选、审核进度、置信度、临时裁剪、缓存、日志和指标是过程或运行产物。

### 4.5 训练、算力与模型辅助标注

- 提交训练时冻结实际图片、完整标签、任务模板和代码边界。用户后续继续上传、移除或修改标签只影响下一次训练，不改变已提交运行。
- ClearML 只负责训练排队、执行、记录和产物；它不拥有平台数据集真值。未来多台闲置机器各自执行独立任务，不为单次训练建设跨机器分布式训练。
- 训练日志和实验分析不是当前主要痛点。平台初期只从 ClearML 提供用户推进任务所需的状态、基本进度、失败信息和模型结果，不建设复杂实验分析界面。
- 已有模型可以为人工标注生成候选标签，但候选结果必须检查、补漏和修正后才能成为正式标签。模型辅助标注不允许把未审核预测直接用于训练。
- 半监督训练不在当前路线图内。检测漏检会把真实目标误作背景，现场人员提供的初始标签质量又难以支撑可靠自训练；除非未来有独立现成方案先在真实数据上直接证明有效，否则不重新进入规划。

## 5. 架构原则

- **验证通过后再建设平台**：P1 已证明 xxtrain、CVAT 和 ClearML 的实际链路成立，P2 从平台权威数据和生命周期开始。
- **统一产品入口**：用户面对 xxtrain 的固定流程，外部平台只是执行引擎。
- **固定模板、单模型任务**：用受控业务边界换取简单、可靠的使用体验。
- **权威数据最小化**：原图只保存一份，数据集和训练快照通过关系组织；派生产物保持可重建。
- **数据按任务积累**：基准数据按模板跨用户共享，用户补充数据按现场和用户隔离。
- **训练输入不可变**：每次运行冻结实际图片、标签、任务和代码边界，不依赖可变目录代表历史输入。
- **复用而不让渡控制权**：CVAT 和 ClearML 承担专业能力，xxtrain 保留业务与数据真值。
- **管理员低频兜底**：正常用户路径不依赖管理员，基准提升和异常处理按需批量进行。
- **按真实需求扩展**：第一版保持单机、内部使用和有限角色；多机调度只在出现真实需求后加入。
- **转移而非消灭标注成本**：平台把数据准备、标注推进和训练执行分散给现场实施人员，不承诺通过训练算法减少总标注量。
- **统一内部表示**：格式差异停留在输入解析和数据集输出边界。
- **组合优于分支堆叠**：用小型 Processor 组成任务流程，配置只表达稳定参数。

具体数据库表、接口、身份接入、CVAT 页面集成、清洗算法、相似度门限、存储清理、训练产物期限和模型候选接入方式均属于后续阶段设计，不由本文预先规定。
