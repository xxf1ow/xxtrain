# xxtrain 进度控制

> 本文只跟踪当前和近端阶段、依赖关系与验收门禁。长期方向和稳定约束见 `ARCHITECTURE.md`，开发者当前事实见 `CLAUDE.md`，已完成工作的过程记录由 Git 历史保存。

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 待设计 | 目标已进入路线，但边界和验收标准尚未批准 |
| 设计中 | 正在形成可审核设计 |
| 待实现 | 设计已批准，尚未开始实现 |
| 进行中 | 正在实现，尚未满足全部验收门禁 |
| 待验收 | 实现和自动验证完成，等待人工审核 |
| 完成 | 设计、实现、自动验证和人工审核均已通过 |
| 阻塞 | 存在必须先解决的外部依赖或决策 |

## 活跃路线

| 顺序 | 里程碑 | 状态 | 前置条件 | 完成门禁 |
| --- | --- | --- | --- | --- |
| M1 | LabelImg/LabelMe 目标子集转换与标签统计 | 待实现 | 无 | 实现和回归测试完成；现有 Recipe 行为兼容 |
| M2 | 源变化检测与显式强制重建 | 待设计 | M1 的输入边界稳定 | 失效规则获批；不会误用陈旧数据集；可显式重建；失败可恢复 |
| M3 | 最小离线半监督训练核心 | 阻塞 | M1、M2 完成 | 种子训练、伪标签筛选、人工固定验证集、运行产物和停止条件形成可验证闭环 |
| M4 | 固定模板轻量训练平台 | 阻塞 | M3 核心稳定 | Point 二级单模型任务完成前置标注、训练锁、进度反馈和审核回流验收 |
| M5 | 在线 Teacher–Student | 阻塞 | M3、M4 产生稳定收益和评估证据 | 训练策略、Teacher 更新、损失与稳定性控制经独立设计和实验验证 |

当前只激活 M1。M2 可以进行问题收集，但在 M1 输入边界确定前不进入实现；M3、M4、M5 不并行展开。

## 已批准的第二阶段方向

以下方向已经批准，但不表示对应能力已经实现：

- 客户只能选择开发者预定义的任务模板和“快速试训/正式训练”等预设；
- 每个训练任务只训练一个模型，不建设自动多模型 DAG；
- 模板用“完整且逐图审核的前置标注 + 只需种子门限的目标标注”统一普通任务和多级任务；
- 前置标签可通过人工、导入或已有模型生成，但二级训练只依赖合格标签，不依赖一级训练历史；
- 数据集只管理原图、人工标签和人工审核通过的伪标签；
- 候选标签、未审核伪标签、审核进度、临时裁剪和训练快照属于可持久化或可重建的过程产物；
- 训练开始后锁定图片和正式标签，修改前必须中断训练；
- Point 二级任务强制逐图完成全部一级框，二级标签只需种子集；
- 一个 Point 框只保存一次，`Point` 是未细分类的一级标签，`tl/tc/cl/cc` 是其二级分类；
- Point 一级框禁止重叠，每个合法框必然存在真实二级目标；
- 第一版产品以 Point 二级分类或分割的独立单模型任务完成端到端闭环为代表性验收；
- 每类人工种子硬门限保留，相似度如何调整额外种子量待真实数据实验确定。

M3 先在现有训练核心中验证离线半监督数据流和评估有效性；M4 只在核心稳定后增加薄平台、进度反馈和标注工作区，避免同时调试算法与产品流程。

## M1：LabelImg/LabelMe 目标子集转换与标签统计

### 当前事实

- `DirectorySource` 已按图片稳定发现样本；
- 标准标注管线已按同名规则查找 `anns/<name>.xml` 和 `anns_seg/<name>.json`；
- reader 会读取文件中的全部 LabelImg object 或 LabelMe shape，并在任务边界验证；
- 普通目录源不生成 `LabelCatalog`，标准 Recipe 仍由 `src/labels.txt` 明确本次任务的目标类别；
- `FilterLabels` 已能过滤目标列表以外的标注，但报告只保存标签集合，不能给出各标签数量或验证最终类别覆盖；
- YOLO 文件只保存类别编号，无法脱离现有目录恢复类别名称；
- COCO 使用自身 category schema，特殊 Recipe 使用显式目录和既有转换规则，不依赖目录源自动发现类别。

### 已确认设计

1. 不根据 LabelImg/LabelMe 中出现的标签自动决定训练类别；
2. 标准任务继续由 `labels.txt` 明确目标类别，显式 Recipe 继续使用自己的固定类别和转换规则；
3. 转换时统计全部来源标签，目标列表之外的标签直接过滤并报告，不阻断转换；
4. 完整运行现有处理链后统计最终输出类别，任一目标类别为零时转换失败且不启动训练；
5. 标签统计本身不引入预扫描，读取、过滤、转换和计数在同一次遍历中完成；
6. XML/JSON 解析失败、几何类型不兼容和 sink 写入失败保持现有立即失败语义；
7. 不增加标签编辑器、显式忽略标签列表、暂存索引、两阶段导入或新的平台确认步骤。

特殊 Recipe 的来源标签与最终模型类别可以不同。来源统计发生在过滤前，最终类别校验发生在既有过滤、重命名、裁剪等处理之后，避免让统计逻辑重复理解业务转换规则。

### 明确不做

- 不把自动发现到的全部标签写入 `labels.txt`；
- 不根据字典序、出现顺序或频次自动生成训练类别目录；
- 不把来源标签映射建设成通用配置系统；
- 不为了聚合所有结构错误而改变整个管线的异常边界；
- 不处理失败后残留产物、来源变化或缓存失效，这些属于 M2。

### 验收门禁

- 独立设计文档经人工审核后，状态才能变更为“待实现”；
- LabelImg 和 LabelMe 混合多个标签时，只转换目标子集并准确报告目标与忽略数量；
- 所有来源标签均进入稳定排序的统计报告，Unicode 和数字标签得到覆盖；
- 任一最终目标类别为零时转换失败，训练工作流不会继续启动；
- 特殊 Recipe 按处理后的最终类别校验，不把原始标签误当作模型类别；
- 现有 `labels.txt`、显式特殊 Recipe、COCO、分类和纯 YOLO 数据路径保持兼容；
- 现有解析错误和 sink 错误继续立即失败；
- 全量单元测试、Ruff、格式检查、compileall 和 `git diff --check` 通过；
- `ARCHITECTURE.md` 与 `CLAUDE.md` 在能力落地后同步更新。

## 更新规则

- 每次只保留一个活跃里程碑；
- 状态变化必须附带可复核证据，不用代码存在代替验收完成；
- 设计未批准前不得把“待确认决策”写成既定实现；
- 完成里程碑后，仅将长期有效的结论提取到 `ARCHITECTURE.md` 或 `CLAUDE.md`，过程细节留在 Git；
- 新阶段开始时删除已完成的详细清单，避免本文再次变成历史档案。
