# xxtrain 进度控制

> 本文只跟踪当前和近端阶段、依赖关系与验收门禁。长期方向和稳定约束见 `ARCHITECTURE.md`，开发者当前事实见 `CLAUDE.md`，已完成工作的过程记录由 Git 历史保存。

## 状态定义

| 状态 | 含义 |
| --- | --- |
| 未激活 | 方向已经进入路线，但前置阶段尚未完成 |
| 待设计 | 前置条件满足，尚未批准本阶段边界和验收标准 |
| 设计中 | 正在形成可审核设计 |
| 待实现 | 设计已批准，尚未开始实现 |
| 进行中 | 正在实现，尚未满足全部验收门禁 |
| 待验收 | 实现和自动验证完成，等待人工审核 |
| 完成 | 设计、实现、自动验证和人工审核均已通过 |
| 阻塞 | 存在必须先解决的外部依赖或决策 |

## 活跃路线

| 顺序 | 里程碑 | 状态 | 前置条件 | 完成门禁 |
| --- | --- | --- | --- | --- |
| P1 | xxtrain、CVAT、ClearML 最小可行性验证 | 完成 | 基础训练核心可用 | CVAT 标注可进入 xxtrain；ClearML Agent 执行真实训练；状态和 ONNX 可由程序取回 |
| P2 | 平台数据基础 | 待设计 | P1 通过 | 权威图片、标签、工作区、基准数据和训练快照具有明确生命周期，并通过最小实现验收 |
| P3 | 用户自助闭环 | 未激活 | P2 数据边界稳定 | 用户通过 xxtrain 单一入口完成上传、标注、训练和模型获取，不依赖管理员逐次推进 |
| P4 | 基准数据积累 | 未激活 | P3 闭环稳定 | 管理员可低频把合格用户数据提升为隐藏基准数据，不复制图片且不阻塞用户任务 |
| P5 | 按需扩展闲置算力 | 未激活 | 出现真实排队或算力需求 | 新增 ClearML Agent 不改变用户流程和平台数据所有权 |

当前只激活 P2。P3 可以进行问题收集，但在 P2 的权威数据和生命周期确定前不进入实现；P4、P5 由真实使用需求触发，不提前建设。

## P1：最小可行性验证（完成）

### 验证结果

- 使用 4 张图片和单类别 `point-detect` 任务创建 CVAT Job，人工完成 4 个框并将 Job 标记为完成；
- CVAT 导出的 COCO 数据包含 1 个类别、4 张图片和 4 条标注；
- xxtrain 将其转换为 3 张训练图片和 1 张验证图片；
- ClearML Agent 从 `default` 队列领取任务，在真实 GPU 上执行一轮 xxtrain 训练；
- ClearML 任务正常完成并保存 ONNX，产物可通过接口重新下载且通过 ONNX 结构校验。

### 得出的结论

1. CVAT 作为标注引擎、ClearML 作为训练执行与记录引擎、xxtrain 作为中间控制层的职责划分成立；
2. 三方 API 和数据格式之间不存在阻断平台路线的缺口，P2 可以开始；
3. 第三方原生页面不能组成普通用户流程：CVAT 完成入口隐蔽且完成后不返回，ClearML UI 信息过多；
4. xxtrain 必须提供唯一入口并承接步骤门禁、状态轮询、失败反馈和页面跳转；
5. 不维护 CVAT 或 ClearML 的中文分支。ClearML 保持管理员后台，CVAT 只暴露必要的标注界面。

## P2：平台数据基础

### 本阶段要解决的问题

在接入普通用户流程前，xxtrain 必须先拥有独立于 CVAT 和 ClearML 的权威数据边界。否则同一图片可能被多次复制，用户移除、自动过滤、人工标签、基准数据和训练输入会互相覆盖，后续也无法解释一次训练实际使用了什么。

P2 只建立支撑后续闭环所需的最小数据基础：

- SQLite 保存任务模板、用户、工作区、图片状态、标签状态、外部任务映射和训练快照关系；
- 权威原图按内容寻址只保存一份，工作区、基准数据和训练快照只保存引用；
- 用户数据按用户、任务模板和现场工作区隔离，基准数据按任务模板对用户隐藏并跨用户共享；
- 用户移除、模糊过滤、相似度过滤和标注状态保持独立语义，不立即删除物理图片；
- 提交训练时冻结图片、标签、模板和代码边界，后续编辑只影响下一次训练。

### 设计边界

- 本阶段不建设完整用户自助 UI，只实现验证数据边界所需的最小入口或临时调用；
- 不实现自动清洗算法、CVAT 页面集成、ClearML 进度页面或管理员基准提升流程；
- 不建设通用数据版本系统、复杂权限、计费、跨机器数据迁移或分布式训练；
- 不恢复半监督训练规划，也不通过未审核伪标签减少标注量；
- 不为 CVAT 或 ClearML 做汉化。

### 进入设计前需要明确

- 权威图片的内容标识、落盘布局和引用规则；
- 工作区、图片逻辑状态、正式标签、基准关系和训练快照之间的最小状态转换；
- 哪些一致性约束必须由 SQLite 保证，哪些文件产物允许失败后重建；
- P2 最小验收场景及其失败恢复边界。

具体表结构、接口和实现顺序在 P2 设计中确定，不由进度文档预先规定。

## 更新规则

- 每次只保留一个活跃里程碑；
- 状态变化必须附带可复核证据，不用代码存在代替验收完成；
- 设计未批准前不得把待确认决策写成既定实现；
- 完成里程碑后，仅将长期有效的结论提取到 `ARCHITECTURE.md` 或 `CLAUDE.md`，过程细节留在 Git；
- 新阶段开始时删除已完成阶段的详细过程清单，只保留影响后续工作的验证结论。
